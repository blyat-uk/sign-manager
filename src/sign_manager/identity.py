"""Every user-visible and on-disk name for the app, in one place."""

from __future__ import annotations

APP_NAME = "Sign Manager"
APP_SLUG = "sign-manager"
ORG_NAME = "BGPP"
BUNDLE_ID = "uk.blyat.sign-manager"
REPO_URL = "https://github.com/blyat-uk/sign-manager"

# Names used before the rename; only the settings migration reads them.
LEGACY_SLUG = "sub-label-pos"
LEGACY_QSETTINGS_SCOPE = ("SubLabelPos", "SubLabelPos")
